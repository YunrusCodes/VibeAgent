# 花園澆灌 Agent 規格

> 題目三。植物狀況＋天氣預報＋溫濕度感測 → 排程灑水施肥。
> 依 `Skills/five-layer-agent/assets/spec-template.md` 產出。**未定案**——第 10 節待你決定。

---

## 1. 這題與前兩題的根本差異

| | 科普帳號 | 記帳 | **花園** |
|---|---|---|---|
| 不可逆動作的性質 | 說出去收不回 | 寫進帳本 | **物理的** |
| 最壞後果 | 聲譽受損，可更正 | 帳本失真 | **植物死亡，不可逆** |
| 有無累積效應 | 無 | 無 | **有——施肥會累積** |
| 執行時有無人在場 | 有 | 有 | **無（半夜排程）** |
| 輸入可信度 | 自己產生 | 使用者提供 | **感測器會壞、預報會錯** |

前兩題的邊界層是形式上的，這題的邊界層是**真的在擋災難**。

---

## 2. 分層決策

| 層 | 保留 | 理由 |
|---|---|---|
| 引導 | 是 | 恆真 |
| 調度 | **是** | 有真實分支：澆不澆、澆多少、要不要施肥、何時執行 |
| 邊界 | **是（核心）** | 兩個不可逆物理動作 |
| 演算 | 是 | L3 觀察 + L2 研判 + L0 計算 |
| 資料 | 是 | 累積劑量是**安全關鍵**狀態 |

最高確定性等級：**L3**（看照片判斷植物狀況）
權力分立是否必要：**是**

**這是三題裡第一個五層全開、一層都不塌縮的。** 前兩題塌了三層與一層，這題塌不掉——因為權力真的散在五個地方。

子 agent：無。meta-agent：無（見第 10 節第 3 題）。

---

## 3. 四個真正的風險

**（一）感測器卡死。** 濕度讀數卡在 30% 不動，系統永遠認為缺水，澆到根腐。**壞掉的讀數看起來跟正常的一模一樣**——這是本題最陰險的失敗。

**（二）劑量累積。** 施肥不是單次事件。今天施了、明天判斷「土壤養分不足」再施一次，兩次都「合理」，合起來是過量。

**（三）L3 直接觸發不可逆動作。** 「照片看起來葉子有點黃」是自由推理，澆水是物理動作。H-15 明文禁止這條路徑。

**（四）無人在場時的拒絕無處可送。** 半夜三點系統不確定，同步回傳沒有人接。這是框架目前未涵蓋的情況，見第 9 節。

---

## 4. 引導層

版本：`v1`

目標：依可信證據維持植物的水分與養分，寧可不足勿過量。

| ID | kind | risk | 條文 | scope | hard_check |
|---|---|---|---|---|---|
| C-1 | grant | high | 可執行澆水，單次不得超過上限 | boundary | `dose-within-limit` |
| C-2 | grant | high | 可執行施肥，單次不得超過上限 | boundary | `dose-within-limit` |
| C-3 | forbid | high | 週期累積劑量達上限後不得再執行 | boundary | `cumulative-budget` |
| C-4 | forbid | high | 不得依據未通過健康檢查的感測器讀數執行動作 | boundary | `sensor-coherence` |
| C-5 | require | high | **不確定時預設不動作** | dispatch | `default-deny-on-uncertainty` |
| C-6 | forbid | high | L3 觀察不得直接觸發執行，須經 L2 結構化研判並附證據 | compute, dispatch | `evidence-required` |
| C-7 | require | low | 天氣預報僅為證據之一，不得作為唯一依據 | compute | — |

### C-5 是這份規格的骨幹

失敗方向必須是安全的那一側：

```
少澆  → 植物渴，可恢復
多澆  → 根腐，可能不可恢復
```

所以**所有不確定都倒向不動作**。這是 H-5 的精神——把「判斷會錯」這個本質缺陷擋在無害的那一側。

---

## 5. 邊界層

| action_id | reversible | idempotency_key | precheck（純函式） | grant |
|---|---|---|---|---|
| `water` | **false** | `date + zone + slot` | 劑量 ≤ 單次上限；累積 ≤ 週期上限；證據自洽；近 N 小時無降雨紀錄 | C-1 |
| `fertilize` | **false** | `date + zone` | 同上，且累積上限更嚴 | C-2 |
| `read-sensor` | true | — | — | — |

### precheck 驗的不只是參數，還有證據

這是本題最重要的設計決定：

> **執行指令必須附上它所依據的讀數，`precheck` 驗證那些讀數的自洽性。**

`sensor-coherence` 是純函式，驗物理不可能性——不需要任何理解能力：

| 檢查 | 判定 |
|---|---|
| 讀數卡在同一值超過 N 小時 | 卡死 |
| 變化率超過物理極限 | 故障 |
| 上次澆水後濕度未上升 | 感測器或管路異常 |
| 多顆感測器互相矛盾 | 至少一顆壞了 |

任一觸發 → `block`，且依 C-5 預設不動作。

**這讓 H-4「邊界層不做語意判斷」不變成「邊界層很笨」**——它不理解植物，但它算得出物理上不可能的事。

### 劑量預算

```
water_ml_per_day        每區每日上限
fertilizer_g_per_week   每區每週上限
```

這是 `boundary_calls` 的推廣。**就算前面所有判斷都錯了，它今天最多澆 X ml。** 與科普帳號的「每天最多發 3 篇」是同一個機制——不依賴任何理解能力的上界。

---

## 6. 演算層

| unit_id | level | input | output |
|---|---|---|---|
| `observe` | **L3** | 植物照片 | 外觀描述（**不得含動作建議**） |
| `sensor-health` | **L0** | 讀數序列 | `pass` / `suspect` + 理由 |
| `forecast` | **L1** | — | 未來 N 小時降雨機率 |
| `assess` | **L2** | 以上三者 | `{action, dose, evidence[], confidence}` |
| `schedule` | **L1** | assess 結果 | 執行時段 |

無狀態，無 store handle。

### `observe` 的輸出不得含動作建議

這是 C-6 的實作形式。L3 只描述它看到什麼（葉片顏色、萎垂程度），**不說該澆水**。要不要澆是 `assess` 這個 L2 單元的事，而它的輸出必須帶 `evidence[]`——每個結論都要指回具體讀數或觀察。

`evidence-required` 硬檢查：`evidence[]` 為空 → 拒絕執行。

**L3 → L2 → precheck → L0 執行。** 中間隔了兩道可驗證的關卡。

---

## 7. 資料層

| key | 內容 | 壽命 | 可壓縮 |
|---|---|---|---|
| `__readings` | 感測器歷史 | 永久 | **否**——健康檢查要用序列 |
| `__doses` | 累積劑量 | 滾動週期 | **否** |
| `__actions` | 執行紀錄 | 永久 | **否** |
| `__pending` | 待人類確認 | 至結清 | **否** |
| `__refusals` | 拒絕紀錄 | 永久 | **否** |

介面只有 `read` / `append` / `annotate`。`__doses` 是**安全關鍵**狀態——它若被改寫，累積上限就失效了。

---

## 8. 調度層

```
每個排程時點：
  read-sensor → sensor-health
      ├ suspect → 依 C-5 不動作 → append __pending → 通知人類（非同步）
      └ pass ↓
  observe（若有照片）+ forecast
      ↓
  assess → {action, dose, evidence[], confidence}
      ├ confidence < 閾值 → 依 C-5 不動作 → __pending
      └ ≥ 閾值 ↓
  vet（事前自審：劑量、累積、證據非空）
      ├ 不通過 → refuse，引用條目 ID
      └ 通過 ↓
  schedule → water / fertilize（precheck → execute）
      └ block → 計數，回到不動作
```

---

## 9. 這題暴露了框架的一個缺口

H-11 要求拒絕「同步回傳 + 非同步留痕」，理由是只寫進資料層等於黑洞。

**但半夜三點沒有人接同步回傳。**

框架假設了父層存在且會收。這題不成立——執行時無人在場。目前的處理是：

> **當拒絕無法同步送達時，預設動作必須是最保守的那一個。**

在這題就是「不動作」。這條規則不在 H-1…H-13 裡，是本題長出來的。

**已收進 five-layer-agent**（簽署於 2026-08-27，正式編號 [H-14]），原提案如下：

```
[H-14] require | scope: dispatch | risk: high
  拒絕的同步回傳無接收者時，預設動作為最保守選項。
  「最保守」必須在引導層明確定義，不得由執行期判斷。
  hard_check: default-action-declared
```

最後那句是重點：**哪個動作最保守，必須是開發期寫死的，不能讓模型當場想。**

---

## 10. 未決事項（你決定，agent 不得代決）

1. **劑量上限的實際數值**——單次、每日、每週，每種植物各一組。這是安全參數，不是調校參數。
2. **`__pending` 怎麼結清**——隔天自動作廢、累積等你處理、或到閾值就強制通知？作廢最安全但會漏，累積會讓你某天面對一堆待辦。
3. **要不要修法迴路（meta-agent）。** 一年跑下來規則必然要調（季節、植物長大）。理由與科普帳號的③相同。但可以先不做，等真的累積出拒絕紀錄再說——**先手做一遍，再抽象**。
4. **植物種類怎麼表示**——參數（植物檔案）還是各自的引導層？我傾向參數：不同植物只是數值不同，規則結構相同。做成引導層就是為了湊架構而分層（checker 的 J8）。
5. **哪些條文永遠不可 `loosen`。** 建議 C-3、C-4、C-5 全部鎖死。
