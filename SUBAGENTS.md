# Subagent 職責清單 — 科普帳號

> 配合 `ARCHITECTURE.md` 讀。這裡展開每個 subagent 的五層。
> **✕ = 塌縮**。塌縮不是偷懶，是在低風險處明確接受權力集中（`SKILL.md`「塌縮規則」節）。
> 判準：任一單元 ≥ L2 才需要完整分立；全部 L0/L1 就退回傳統三層。

固定 subagent **15 個**，handler **3 型**（每則留言動態實例化）。

---

## ① 發文 pipeline — 複合 subagent（7 個）

| # | subagent | 引導層（關鍵條文） | 調度 | 邊界（grants） | 演算 | 資料 |
|---|---|---|---|---|---|---|
| 1 | **選題** | 不得選醫療／投資／法律；90 天內不得重複；被 `refused` 過的題目不得重提 | ✕ | `search-whitelist`（可逆） | L3 候選題目 + 排序 | 讀 `__posted` `__refusals` |
| 2 | **查證** | 每個宣稱須有出處；出處須在白名單域名；預印本不得為唯一依據 | 逐條迴圈 | `search-whitelist`（可逆） | **L2** `{claim, source, supports}` | append 查證表 |
| 3 | **撰稿** | **不得引入查證表以外的宣稱** | ✕ | ✕ | L3 主稿 | 讀查證表 |
| 4 | **圖表規格** | 只能用白名單圖表型別；每個 label 須有 source | ✕ | ✕ | **L2** `{type, data, labels, source}` | 讀查證表 |
| 5 | **平台改寫** | 只改形式不改事實；各平台字數／格式上限 | 逐平台迴圈 | ✕ | **L2** 各平台版本 | 讀主稿 |
| 6 | **審稿** | **存疑即退**；不得放行無出處宣稱 | pass / reject | ✕ | **L2** `{claim, cited, verdict}` | append 審稿紀錄 |
| 7 | **發布** | 一平台一條 grant；每日上限 | 決定發哪些平台 | **`publish-threads` `publish-x` `publish-fb`**（不可逆） | ✕ | append `__posted` |

### 這條線上的四個設計要點

**（一）撰稿的禁區只有一條，但它是最重要的一條。**
L3 寫作最容易「順手」補一句看起來對的事實。硬檢查做得到：稿件中每個數字與宣稱都必須能對回查證表，對不上就退。這讓 L3 的自由度被限制在**表達**，而不是**內容**。

**（二）審稿的引導層必須與撰稿相反。**
撰稿寫「盡量完整可讀」，審稿寫「存疑即退」。**這種張力放在同一個 agent 裡是缺陷，放在兩個 agent 裡才是設計。** 這也是它們必須是兩個 subagent 而不是一個的唯一理由。

**（三）圖表規格產出的是資料，不是圖。**
渲染器是固定的 L0 程式，不是 subagent。這樣硬檢查才驗得了——檢查對象是資料不是像素。代價：渲染器能表達什麼，圖就最多能有多豐富。

**（四）整條線只有「發布」有不可逆動作。**
其他六個就算全部失控，最壞後果是產出一篇爛稿，而它會被審稿擋下，再被 precheck 擋下，再被每日上限擋下。三道，一道比一道笨，一道比一道擋得住。

### 發布的邊界層（唯一需要展開的）

```
precheck  純函式，不含模型呼叫
  ├ 每個宣稱的 citation 欄位非空
  ├ citation 域名 ∈ 白名單
  ├ 不含醫療宣稱詞表
  ├ 平台字數上限
  └ 圖表 type ∈ 白名單

execute   呼叫平台 API
  idempotency_key = date + platform + content_hash

budget    boundary_calls: 3 / 日，不重置
```

**budget 是最後一道。** 就算前面所有語意約束全部失效，它今天最多發三篇。這是不依賴任何理解能力的上界。

---

## ② 留言處理 — meta-agent（4 個 + 3 型 handler）

| # | subagent | 引導層（關鍵條文） | 調度 | 邊界 | 演算 | 資料 |
|---|---|---|---|---|---|---|
| 8 | **分流** | 輸出必須是受限型別之一，**不得自創型別** | ✕ | ✕ | **L2** enum | 讀原文脈絡 |
| 9 | **組閣** | **M-1 不得產出自身的引導層**；grants 只能從父層挑；forbid 只能加 | 選模板 + 收窄 | ✕ | **L2** 輸出 `Guidance` | 讀 handler 模板庫 |
| 10 | **審查** | 存疑即拒；**只有否決權，沒有指導權** | pass / reject | ✕ | **L0 機械 + L2 語意** | append 審查紀錄 |
| 11 | **編成** | 只接受已通過的草案 | ✕（近 L0） | **`spawn-subagent`**（不可逆） | ✕ | append 生成紀錄、`depth` |

### handler 三型（動態生成，用完即棄）

| 型別 | goal | grants | 額外 forbid |
|---|---|---|---|
| **技術提問** | 依白名單來源回答本文範圍內的問題 | `search-whitelist` `reply-comment` | 不得回答超出本文主題的問題 |
| **敵意** | 判定並回報 | **（空）** | 不得回覆 |
| **指錯** | 產出更正草案 | `propose-correction` | 不得直接修改已發布內容 |

**敵意 handler 沒有回覆動作**——不是「被要求不要回覆」，是它根本沒有那個能力。它可以被說服，說服了也沒用。H-4 用在權限配置上。

它剩下的唯一通道是**回報權**，那是 H-9，narrowing 不得收窄。少了這條，它會變成一個既不能回覆、也不能說自己不能回覆的死結。

### 三個設計要點

**（一）分流的輸出必須是受限集合。**
若分流能自由描述留言類型，它實際上就在決定權限了，而它沒有被審查。受限 enum 把「分類」和「授權」切開。

**（二）審查有兩段，L0 先跑。**
`narrowing` 檢查（M8 `grants ⊆ 父`、M10 `forbid` 條目全保留且 ID 不變、M9 `budget ≤ 父`）是**機械的**，寫成單元測試，不需要 LLM。語意檢查（這份 guidance 合不合理）才是 L2，而且是第二道。

**（三）更正稿走跟原文同一條發布管線。**
指錯 handler 只能 `propose-correction`，草案要重新過審稿與 precheck。**更正不能比原文更容易發出去**，否則那就是繞過審查的後門。

---

## ③ 修法迴路 — meta-agent（4 個，週循環）

| # | subagent | 引導層（關鍵條文） | 調度 | 邊界 | 演算 | 資料 |
|---|---|---|---|---|---|---|
| 12 | **彙整** | **不得改寫 `__refusals`**，只讀 | ✕ | ✕ | **L0／L1** 計數、分組 | 讀 `__refusals` `__amendments` 成效 |
| 13 | **起草** | 拒絕不預設為缺陷，先分三類；**不得代擬 `worst_case`** | 先分類再定 direction | ✕ | **L2** 輸出 `Amendment` | 讀彙整結果 |
| 14 | **審查** | 檢查 `direction` 有無被誤標 | pass / reject | ✕ | **L2** | append 審查紀錄 |
| 15 | **呈遞** | `worst_case` 必須留空 | ✕（近 L0） | `notify-human`（可逆） | ✕ L1 格式化 | 讀 `__amendments` |

### 三個設計要點

**（一）彙整是 L0／L1，不是 LLM。**
這是整份清單最容易放錯的一格。「同一個 `clause_id` 出現幾次」「拒絕率變化」都是計數，不需要理解能力。放 LLM 進去只會讓統計變得不可重播——而這條線的產物是要拿去改憲法的，它必須可稽核。

**（二）起草的第一個動作是分類，不是修法。**
`dev-loop.md:21` 的三分類：約束正確（**什麼都不做**）／約束太緊（`loosen`／`clarify`）／目標本身有問題（`retarget`）。第三類最常被誤判成第二類，因為拆掉護欄比縮小目標更快讓任務完成。

**（三）呈遞的產物是「累積 diff」，不是「這次的改動」。**
對照**創建版**而非上一版。逐次審查一定會通過，累積審查才擋得下來。

### 修法審查要特別看的一件事

`direction` 被誤標是最隱蔽的漏洞：一個實質是 `retarget`（該縮小目標）的提案，被寫成 `clarify`（可自動套用），就繞過了人類簽署。所以審查 subagent 的核心工作不是判斷提案好不好，是**判斷它被歸在對的方向**。

---

## 塌縮統計

| 層 | 保留 | 塌縮 |
|---|---|---|
| 引導 | 15 / 15 | 0 — **永不省略** |
| 調度 | 7 | 8 |
| 邊界 | 6 | 9 |
| 演算 | 13 | 2 |
| 資料 | 14 | 1 |

**多數 subagent 只有 3 層。** 完整五層的只有「發布」與「編成」——正好是兩個有不可逆動作的。

這是塌縮規則運作正常的樣子：**權力集中在哪裡，結構就在哪裡展開。**

## 不可逆動作總表

只有三個，全部 allowlist：

| 動作 | 屬於 | idempotency_key | budget |
|---|---|---|---|
| `publish-*` | 發布 | date + platform + hash | 3 / 日 |
| `spawn-subagent` | 編成 | comment_id | 依 depth 上限 |
| `propose-correction` | 指錯 handler | comment_id | 走發布管線 |

`search-whitelist`、`reply-comment`、`notify-human` 是可逆的，用 denylist。

**漏寫一條禁止，最多多做一次無害推理；漏寫一條授權，它就是發不出那篇文。失敗方向是安全的。**
